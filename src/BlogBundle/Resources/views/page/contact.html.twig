{# src/Blogger/BlogBundle/Resources/views/Page/contact.html.twig #}
{% extends 'BlogBundle::layout.html.twig' %}

{% block title %}Contact{% endblock%}

{% block body %}
    <header>
        <h1>Contact Arazora</h1>
    </header>
{# 	{% if app.session.('blogger-notice') %}#}
{# 	    <div class="blogger-notice">#}
{# 	        {{ app.session.flash('blogger-notice') }}#}
{# 	    </div>#}
{# 	{% endif %}#}
	
    <p>A suggestion?</p>

    <form action="{{ path('BlogBundle_contact') }}" method="post"  class="blogger">
        {{ form_errors(form) }}

        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.subject) }}
        {{ form_row(form.body) }}

        {{ form_rest(form) }}

    </form>
    
{% endblock %}